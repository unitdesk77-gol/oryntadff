{
  "name": "orynta-reconcile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "python3 -m http.server 3000 --directory public",
    "build": "node build.mjs"
  }
}